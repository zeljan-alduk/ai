{
  "name": "@aldo-ai/billing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Stripe-backed subscription, checkout, portal, webhook + trial-gate helpers for ALDO AI. Boots in placeholder mode when Stripe env vars are unset.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc"
  },
  "dependencies": {
    "@aldo-ai/api-contract": "workspace:*",
    "@aldo-ai/storage": "workspace:*",
    "@aldo-ai/types": "workspace:*",
    "stripe": "^17.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@electric-sql/pglite": "~0.4.4",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
