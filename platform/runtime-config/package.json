{
  "name": "@meridian/runtime-config",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Thin glue: load .env into a typed runtime config (LLM-agnostic).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc"
  },
  "dependencies": {
    "@meridian/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
