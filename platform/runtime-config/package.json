{
  "name": "@aldo-ai/runtime-config",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Thin glue: load .env into a typed runtime config (LLM-agnostic).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "build": "tsc"
  },
  "dependencies": {
    "@aldo-ai/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3",
    "vitest": "^2.1.4"
  }
}
