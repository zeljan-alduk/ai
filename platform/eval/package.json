{
  "name": "@aldo-ai/eval",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Eval-harness primitive: suite loader, evaluators, sweep runner, promotion gate. LLM-agnostic: target models are opaque provider.model strings; the gateway picks the actual descriptor at routing time.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc"
  },
  "dependencies": {
    "@aldo-ai/api-contract": "workspace:*",
    "@aldo-ai/engine": "workspace:*",
    "@aldo-ai/gateway": "workspace:*",
    "@aldo-ai/registry": "workspace:*",
    "@aldo-ai/types": "workspace:*",
    "yaml": "^2.6.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
