{
  "name": "@aldo-ai/local-discovery",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Probe well-known local-LLM ports (Ollama, vLLM, llama.cpp, LM Studio) and merge results into the gateway model registry.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc"
  },
  "dependencies": {
    "@aldo-ai/gateway": "workspace:*",
    "@aldo-ai/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
