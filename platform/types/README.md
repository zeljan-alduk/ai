# @aldo-ai/types

Cross-package type contracts for ALDO AI. **Types only, no runtime.**
Every other package imports from here; nothing in here imports from
anywhere else.

The contracts are drawn directly from
`docs/adr/0001-agent-spec-and-engine-interfaces.md`. If you need to change
a contract, update the ADR first.
