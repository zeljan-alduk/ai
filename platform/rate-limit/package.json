{
  "name": "@aldo-ai/rate-limit",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Distributed token-bucket rate limiter (Postgres-backed) for the ALDO AI control plane. LLM-agnostic — provider names never appear here.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc"
  },
  "dependencies": {
    "@aldo-ai/storage": "workspace:*",
    "hono": "^4.12.16"
  },
  "devDependencies": {
    "@electric-sql/pglite": "~0.4.4",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
