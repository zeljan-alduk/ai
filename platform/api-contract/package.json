{
  "name": "@aldo-ai/api-contract",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Wire-format contract for the ALDO AI control-plane API. Zod schemas + types shared by apps/api and apps/web.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "build": "tsc"
  },
  "dependencies": {
    "@aldo-ai/types": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
