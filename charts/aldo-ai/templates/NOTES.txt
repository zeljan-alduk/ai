{{/* Licensed under FSL-1.1-ALv2 */}}

ALDO AI {{ .Chart.AppVersion }} installed in namespace {{ .Release.Namespace }}.

Release name: {{ .Release.Name }}

Pods (give them a minute):
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

{{- if .Values.ingress.enabled }}

Web app:    https://{{ .Values.ingress.hosts.web }}
API:        https://{{ .Values.ingress.hosts.api }}
{{- else }}

No ingress configured. Port-forward to reach the web app:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "aldo-ai.fullname" . }}-web 8080:{{ .Values.service.web.port }}
  open http://localhost:8080
{{- end }}

{{- if .Values.privacyTier.enforced }}

Privacy-tier enforcement: ON
  Pods labelled aldo.tech/privacy-tier=sensitive are network-isolated
  from cloud-model APIs by NetworkPolicy. Verify your CNI enforces
  NetworkPolicy (Calico / Cilium / Antrea / EKS+VPC-CNI w/ NP plugin /
  GKE Dataplane V2 / AKS w/ Azure NP).
{{- else }}

Privacy-tier enforcement: OFF
  Set privacyTier.enforced=true to network-isolate sensitive pods from
  cloud-model APIs. CLAUDE.md non-negotiable #3.
{{- end }}

{{- if not .Values.secrets.bringYourOwnSecret }}

WARNING: secrets.bringYourOwnSecret=false. Provider keys live in the
Helm release manifest. Switch to BYO Secret + ExternalSecretsOperator /
SealedSecrets for production.
{{- end }}

Docs:    https://ai.aldo.tech/docs
Source:  https://github.com/aldo-tech-labs/aldo-ai
Status:  https://ai.aldo.tech/status
