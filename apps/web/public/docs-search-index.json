[
  {
    "path": "/docs",
    "title": "Documentation",
    "summary": "Quickstart, concepts, guides, API reference, and SDKs for the ALDO AI control plane.",
    "headings": [],
    "body": "aldo ai is the llm-agnostic ai sub-agent orchestrator. define agents as yaml, route to any model, swap cloud and local without touching code, replay every run, and enforce privacy at the platform layer. these docs are organised into five sections: - quickstart — sign up, seed the default agency, run your first agent. - concepts — mental models that shape every part of the platform. - guides — task-oriented walkthroughs for common workflows. - api reference — auto-generated from the zod contract package. - sdks — typescript and python clients. if you read only one page, read the quickstart. if you read two, follow it with capability-class routing."
  },
  {
    "path": "/docs/quickstart",
    "title": "Quickstart",
    "summary": "Sign up, seed the default agency, run an agent, and inspect the trace — in five minutes.",
    "headings": [
      "1. create an account",
      "2. seed the default agency",
      "3. run an agent",
      "4. inspect the trace",
      "5. add an eval gate",
      "what's next"
    ],
    "body": "this is the shortest path from \"never used aldo ai\" to \"watched a multi-agent run replay end to end\". you'll need a browser and (optionally) a model api key — local-only models work too. open the control plane and choose sign up. verify your email and you'll land on the welcome screen of a fresh tenant. the control plane ships with a reference organization — the one aldo tech labs uses to dogfood the platform. from the welcome screen click seed default agency, or run: ```bash curl -x post https://app.aldo-ai.dev/api/auth-proxy/v1/tenants/me/seed-default \\ -h \"authorization: bearer $aldo_api_key\" ``` you now have 26 agents — principal, architect, engineers, code- reviewer, evaluator-builder, and so on. each one is described in the agency concept doc. open the playground from the sidebar. pick the code-reviewer agent and paste a snippet of code. click run. while the run executes, watch the live event stream on the right. tool calls, model tokens, guard hits — all stream in order. when the run finishes, click open run to land on the run detail page. from there: - timeline shows every step in order. - flame graph visualises duration and parallelism. - replay lets you re-execute any ste"
  },
  {
    "path": "/docs/concepts/capability-class-routing",
    "title": "Capability-class routing",
    "summary": "Agents declare capability classes; the gateway resolves a concrete model. Switching providers is a config change, never a code change.",
    "headings": [
      "the classes",
      "why classes, not model names",
      "routing inputs",
      "inspecting decisions",
      "related"
    ],
    "body": "aldo ai is llm-agnostic by charter. no agent names a vendor model. instead, every agent declares a capability class — a coarse-grained tier of reasoning power and budget — and the gateway resolves a concrete model at call time, taking into account the tenant's catalog, the agent's privacy tier, and the agent's budget. there are five classes shipped out of the box: - reasoning-large — frontier models for hard, multi-step reasoning. - reasoning-medium — strong general-purpose reasoning at lower cost. - reasoning-small — fast, cheap, good for routing or extraction. - local-reasoning — runs on the operator's hardware. always allowed for sensitive privacy tier. - fast-draft — for streaming first-token latency. a spec picks one: ```yaml model_policy: capability_class: reasoning-medium fallback: local-reasoning ``` if an agent spec hard-codes gpt-4o, three things break: 1. the spec leaks a vendor decision into the agent layer. a different tenant with a different licensing posture can't run that spec. 2. local mode is impossible. there's no way to swap the cloud model for an apple silicon checkpoint without rewriting the spec. 3. eval comparisons (cloud vs local on the same agent) require "
  },
  {
    "path": "/docs/concepts/privacy-tier",
    "title": "Privacy tier",
    "summary": "A tier on every agent. `sensitive` is a hard gate — those runs physically cannot reach a cloud endpoint.",
    "headings": ["the three tiers", "how it's enforced", "why it's a platform concern", "related"],
    "body": "privacy is enforced at the platform layer, not the agent layer. an agent author cannot accidentally leak data to a cloud provider — the router drops the request before it leaves the box. - public — no constraints. suitable for marketing copy generators, public knowledge agents, and the like. - internal — must stay within the tenant's contracted model set. frontier cloud models are allowed but only those approved by the operator's data-handling policy. - sensitive — physically cannot reach a cloud endpoint. the router filters the catalog down to locality: local providers before scoring, and drops the run if none qualify. every spec carries a privacy_tier field. the router reads it and applies the tier as a hard filter on the catalog. the filter is applied before capability-class scoring, before budget scoring, before health scoring — so even a misconfigured catalog can't leak. ```yaml identity: name: code-reviewer team: support privacy_tier: sensitive model_policy: capability_class: local-reasoning ``` the platform also enforces tier on: - spawned subagents. a sensitive parent can only spawn sensitive children — tier propagates down the tree. - tool calls. tools tagged network are g"
  },
  {
    "path": "/docs/concepts/multi-agent-orchestration",
    "title": "Multi-agent orchestration",
    "summary": "Pipeline, supervisor, parallel, router, debate, and subscription strategies — declared in YAML, run by the engine.",
    "headings": [
      "strategies",
      "pipeline (sequential)",
      "parallel",
      "supervisor",
      "router",
      "debate",
      "subscription",
      "cross-field rules",
      "spawning rules",
      "related"
    ],
    "body": "real workflows rarely fit in a single agent. aldo ai ships six composition patterns that cover the bulk of multi-agent shapes you'll need; pick one (or layer them) on a single composite spec. each subagent receives the previous one's output. use when you have a strict order: extract → summarise → format. ```yaml composite: strategy: sequential subagents: - extractor - summariser - formatter ``` subagents run concurrently against the same input. the aggregator combines their outputs. use when each subagent contributes a different facet (citations, sentiment, key entities). ```yaml composite: strategy: parallel subagents: - citations - sentiment - entities aggregator: combine-facets ``` a supervisor agent decides which subagent runs next and reads results to decide if it's done. use for open-ended planning. a lightweight classifier picks one subagent to run. use for triage (intent classification → specialised handler). two or more subagents argue; an aggregator picks a winner or synthesises a consensus. use for high-stakes generation (legal, medical). ```yaml composite: strategy: debate subagents: - position-a - position-b aggregator: judge ``` a subagent reacts to events from anothe"
  },
  {
    "path": "/docs/concepts/sandbox-and-guards",
    "title": "Sandbox and guards",
    "summary": "Per-agent sandbox policy (network, filesystem) and tool-output guards (spotlighting, output scanner, quarantine).",
    "headings": ["sandbox policy", "tool-output guards", "audit"],
    "body": "tools extend an agent's reach into the real world. sandboxes and guards constrain that reach so an over-eager agent — or a prompt injection — can't do harm. every agent declares a sandbox block on its spec. the block sets ceilings for network and filesystem access. the actual ceiling at runtime is the intersection of the spec's declaration and the operator's runtime config — the spec can never exceed the operator's policy. ```yaml sandbox: network: mode: allowlist allowlist: - api.internal.example.com filesystem: permission: repo-readonly ``` network modes: - none — no network calls. - allowlist — only the listed hosts. - host — full host network. reserved for self-hosted dev only. filesystem permissions: - none — no filesystem access. - repo-readonly — read-only access to the run's working repo. - repo-readwrite — write access scoped to the working repo. - full — full filesystem. reserved for self-hosted dev only. once a tool returns, three guards inspect its output before the result is fed back into the model: - spotlighting — wraps tool output in delimiters the model is trained to treat as untrusted data, reducing prompt-injection risk. - output scanner — checks tool outputs for"
  },
  {
    "path": "/docs/concepts/eval-harness",
    "title": "Eval harness",
    "summary": "Suites, sweeps, evaluators, datasets — the engine for promotion gating.",
    "headings": ["suites", "cases", "evaluators", "sweeps", "promotion gate"],
    "body": "promotion in aldo ai is eval-gated. the registry refuses to promote a new version unless every suite the spec names passes its declared threshold. here is the model. a suite is a declarative bundle of cases plus evaluators plus a threshold. it is itself versioned and lives in the registry. ```yaml suite: name: code-review cases_dataset: dataset:code-review-v3 evaluators: - name: rubric type: llm-judge capability_class: reasoning-large - name: contains type: contains ref: ground-truth threshold: rubric: 0.85 contains: 1.00 ``` a case is a single input (and optional ground-truth) the suite runs the agent against. cases live in datasets — see dataset uploads. built-in evaluator types: - contains — the response contains the expected string. - regex — the response matches a regex. - exact — the response equals the expected value. - llm-judge — score with another agent (configured by capability class, not model name). - script — run a sandboxed js evaluator. custom evaluators are first-class — see custom evaluators. a sweep runs a suite against a matrix of model × spec. use it to compare a frontier cloud model and a local model on the same agent spec — the canonical \"should we ship a loc"
  },
  {
    "path": "/docs/concepts/replayability",
    "title": "Replayability",
    "summary": "Every node boundary is a checkpoint. Edit-and-resume any step against a different model.",
    "headings": ["what's checkpointed", "what you can do with it", "api"],
    "body": "every run is replayable. the engine checkpoints the full message/tool-call history at every node boundary, and any step can be re-executed against a different model, prompt, or tool output. this is what makes aldo ai a debugger-grade orchestrator rather than a \"fire and forget\" sdk. at each step the engine writes: - the system + user messages going into the call. - the model output (assistant message + any tool calls). - the tool results that came back. - the routing decision (which model was chosen, which were skipped, why). - the guard verdicts (which guards ran, what they decided). the full bundle is signed, content-addressed, and durable. the control plane keeps it forever (subject to your retention policy); local-only deployments keep it on disk. - replay — rerun the entire run, deterministically, against the same models. useful when reproducing a regression. - edit-and-resume — change the prompt or tool output at step n, re-execute from n+1. the ui offers a side-by-side diff of the original and edited bundle. - swap model mid-trace — pick a step, choose a different capability class or model, and watch the alternate timeline run to completion. the \"what would have happened on "
  },
  {
    "path": "/docs/concepts/agency",
    "title": "Reference agency",
    "summary": "The dogfood organization shipped with ALDO AI. Use it as the example for every platform feature.",
    "headings": ["layout", "conventions", "promotion", "use it as a test case"],
    "body": "this page is the public-docs version of the agency readme. the agency/ directory in the repository contains the reference set of agent specifications shipped with aldo ai. every file under direction/, delivery/, support/, and meta/ conforms to the aldo-ai/agent.v1 schema. these are the agents the platform itself uses to build, review, and operate products — they are also the canonical examples authors reference when writing new agents for their own tenants. ``` agency/ direction/ principal, strategy, architecture, program management delivery/ engineers who produce artefacts (code, infra, models, pipelines) support/ reviewers, auditors, writers, ops partners meta/ agents about agents (author, evaluator, hr, historian) ``` each agent yaml has a companion system prompt under <team>/prompts/<name>.system.md. prompts stay with their agent so that promoting a new version always moves the spec and the prompt together. - llm-agnostic. specs declare model_policy.capability_class (reasoning-large / reasoning-medium / reasoning-small / local-reasoning / fast-draft); the router resolves a concrete provider at call time. no agent names a vendor model. - privacy first. anything that reads source"
  },
  {
    "path": "/docs/guides/building-an-agent",
    "title": "Building an agent",
    "summary": "From a blank YAML file to a promoted, eval-gated agent in your tenant.",
    "headings": [
      "1. write the spec",
      "2. validate locally",
      "3. register the spec",
      "4. test in the playground",
      "5. author the eval suite",
      "6. promote",
      "patterns"
    ],
    "body": "this guide walks through writing an agent from scratch and getting it eval-gated for promotion in your tenant. agent specs are yaml conforming to the aldo-ai/agent.v1 schema. the minimum fields are identity, model policy, and privacy tier: ```yaml version: 0.1.0 schema: aldo-ai/agent.v1 identity: name: changelog-writer owner: docs team: support role: team: support description: drafts a release changelog from git log + pr titles. privacy_tier: internal model_policy: capability_class: reasoning-medium fallback: local-reasoning tools: - id: git-log - id: gh-list-prs eval: regression: - changelog-quality ``` a companion system prompt lives next to the spec: changelog-writer.system.md. ```bash aldo agent validate ./changelog-writer.yaml ``` the cli uses the registry's parser, so any error you see locally is the same error the server would return. either via the cli: ```bash aldo agent register ./changelog-writer.yaml ``` …or via the api (post /v1/agents with `content-type: application/yaml`). open the playground in the control plane, pick the agent, and run it against a representative input. watch the event stream for unexpected tool calls, and check the routing decision under routing →"
  },
  {
    "path": "/docs/guides/writing-an-eval-suite",
    "title": "Writing an eval suite",
    "summary": "Cases, evaluators, thresholds — the structure of a regression suite.",
    "headings": ["anatomy", "cases", "picking evaluators", "thresholds", "iterating", "versioning"],
    "body": "a suite is a declarative bundle of cases plus evaluators plus a threshold. the registry runs it on every promotion and refuses to move the live pointer when any threshold fails. ```yaml version: 0.1.0 schema: aldo-ai/eval.v1 suite: name: changelog-quality cases_dataset: dataset:changelog-cases-v1 evaluators: - name: rubric type: llm-judge capability_class: reasoning-large rubric: | score 1-5: clarity, completeness, accuracy. - name: must-mention-version type: contains ref: version-tag threshold: rubric: 4.0 must-mention-version: 1.00 ``` cases live in datasets. every row is {input, ground_truth?, tags?}. upload a dataset with the aldo dataset push cli or via post /v1/datasets. - use contains/regex/exact when you have a hard ground truth. cheap, fast, deterministic. - use llm-judge when the rubric is qualitative. always declare it by capability class, never by model name. - use script when the rubric is computable but not regex expressible (e.g. the response is json; check that all required fields are present and well-formed). thresholds are per evaluator. the suite passes only if every evaluator's mean score over the case set meets the threshold. there is no aggregation across eval"
  },
  {
    "path": "/docs/guides/custom-evaluators",
    "title": "Custom evaluators",
    "summary": "Ship your own evaluator. Signature, lifecycle, sandboxing.",
    "headings": ["signature", "lifecycle", "sandboxing", "determinism", "distribution"],
    "body": "the built-in evaluators (contains, regex, exact, llm-judge, script) cover most cases. when you need something custom — a domain-specific scorer, a data-flow check, an external api call — write your own evaluator. ```ts import type { evaluator, evaluatorcontext, evaluatorresult } from '@aldo-ai/eval'; export const myevaluator: evaluator = { name: 'my-evaluator', async score(ctx: evaluatorcontext): promise<evaluatorresult> { const { caseinput, response, groundtruth } = ctx; // ... compute the score return { score: 0.92, label: 'good', detail: 'matched 23/25 fields' }; }, }; ``` the score is always a number in [0, 1]. the label and detail are surfaced in the eval report ui. evaluators are registered once at suite-load time, then called once per case. they are stateless: no global state, no sticky caches, no module-level side effects. the harness runs them in a fresh sandbox per case so a misbehaving evaluator can't poison later cases. custom evaluators run in the same sandbox the engine uses for script tools. network access defaults to none; declare an allowlist on the suite if your evaluator needs to call out: ```yaml evaluators: - name: my-evaluator type: script sandbox: network: mo"
  },
  {
    "path": "/docs/guides/dataset-uploads",
    "title": "Dataset uploads",
    "summary": "Upload JSONL or CSV; bind to suites; share across agents.",
    "headings": ["format", "upload", "privacy tier", "sharing", "inspecting"],
    "body": "datasets are reusable bundles of cases. one dataset can back multiple suites; multiple suites can target one agent. versioning follows the rest of the registry: every push gets a new version, the live pointer moves explicitly. datasets are jsonl or csv. jsonl is preferred — it preserves structured ground-truth fields: ```jsonl {\"input\": \"refactor the auth module\", \"ground_truth\": \"preserved-public-api\", \"tags\": [\"refactor\"]} {\"input\": \"find the off-by-one\", \"ground_truth\": \"loop bound off by one\", \"tags\": [\"debug\"]} ``` csv is fine for flat schemas; the column header row determines the field names. via cli: ```bash aldo dataset push ./changelog-cases.jsonl --name changelog-cases ``` via the api: ```bash curl -x post https://app.aldo-ai.dev/api/auth-proxy/v1/datasets \\ -h \"authorization: bearer $aldo_api_key\" \\ -h \"content-type: application/x-ndjson\" \\ --data-binary @changelog-cases.jsonl ``` datasets carry a privacy tier. a sensitive dataset can only be used by sensitive runs — the platform won't bind it to a suite that runs against a less-strict tier. a dataset is tenant-scoped by default. to share across tenants, the operator must explicitly mark it shared (only available in self"
  },
  {
    "path": "/docs/guides/dashboards-and-alerts",
    "title": "Dashboards and alerts",
    "summary": "Build a dashboard, configure an alert rule, route to integrations.",
    "headings": ["dashboards", "alerts", "silencing", "test fire"],
    "body": "the control plane ships dashboards and alerting out of the box. both are tenant-scoped and built on the same event stream the engine emits. a dashboard is a layout of cards: time-series, leaderboards, eval score histograms, cost-by-capability-class. drag cards to rearrange, resize at the corners. to create one: 1. open the dashboards page from the sidebar. 2. click new dashboard. 3. pick a layout template or start blank. 4. add cards from the catalog. each card binds to a query — pre-built queries are tenant-safe; custom queries are reviewed. dashboards persist to the dashboards table. the wire shape is documented under dashboards in the api reference. an alert is a rule that watches an event type, applies a filter, and fires when a threshold is breached. ```yaml alert: name: nightly-eval-regression source: eval.sweep_completed filter: suite: changelog-quality threshold: metric: rubric.mean op: lt value: 4.0 channels: - integration:slack:ops ``` channels reference integrations by id. the dispatcher fans the alert out best-effort — failures are logged and surfaced in the alert's detail view but don't tear down the run. alerts can be silenced individually for an interval (e.g. during"
  },
  {
    "path": "/docs/guides/integrations",
    "title": "Integrations",
    "summary": "Slack, Discord, GitHub, generic webhooks — secrets-encrypted at rest.",
    "headings": ["configuring", "events", "rbac", "test fire", "pause / resume"],
    "body": "integrations route events from your tenant to external systems. slack, discord, github, and generic webhooks are supported; the runner pattern is the same for all four. open settings → integrations. click new integration, pick a kind, fill in the per-kind config form, and choose the events you want to subscribe to. per-kind config: - slack — bot token + channel id. - discord — webhook url. - github — installation id + repo + signing secret. - generic webhook — url + optional hmac secret. all credentials are encrypted at rest using the tenant's master key. the encryption envelope is {__enc: true, ciphertext, nonce}. production fails closed if the master key is unset; dev gets an ephemeral key with a warning. each integration subscribes to a subset of events: - run.completed - run.failed - eval.sweep_completed - alert.fired - agent.promoted events are best-effort: a misbehaving runner can't tear down a run. failures are logged and surfaced in the integration's detail view. only owners and admins can create or modify integrations. members can view the list (sans secrets) and trigger test fires. every integration has a test button that synthesises a run_completed event and dispatches i"
  },
  {
    "path": "/docs/guides/api-keys",
    "title": "API keys",
    "summary": "Mint scoped API keys; rotate; revoke; audit.",
    "headings": ["scopes", "rotation", "audit", "revocation", "storing keys"],
    "body": "programmatic access to aldo ai uses bearer tokens. mint them under settings → api keys. every key has a scope, an optional expiry, and a last-used timestamp. scopes follow the resource:action shape: - agents:read / agents:write - runs:read / runs:write - eval:read / eval:write - datasets:read / datasets:write - integrations:read / integrations:write - admin:* — owner-level, all scopes. each scope can be granted independently. ci keys typically need runs:write plus the relevant read scopes; never grant admin:* to a ci key. rotate by minting a new key, deploying it, and revoking the old one. the control plane lists every key with its last-used timestamp so you can spot keys nobody is using before you rotate. every api call carries the key id in the audit log. filter the audit log by actor.api_key_id to see what a key has done. revocation is immediate. the next request with the revoked key gets a 401 token_revoked and a structured error. never commit a key. the control plane lets you set an env_alias on each key so a pnpm dev env file knows which environment variable name to expect — no plaintext bytes in the repo."
  },
  {
    "path": "/docs/guides/self-hosting",
    "title": "Self-hosting",
    "summary": "Run ALDO AI on your own infra. Postgres, Redis, the gateway, the API.",
    "headings": ["components", "deploy", "local-models setup", "master key", "backups", "upgrades"],
    "body": "aldo ai is fsl-1.1-alv2 licensed, which lets you self-host for internal use today and use it for any purpose two years after each release. self-hosting is the canonical path for tenants that need sensitive privacy tier. - apps/api — the control-plane http api (hono). - apps/web — the control-plane ui (next.js). - platform/engine — runs the agent graph. - platform/gateway — routes capability classes to concrete models. - postgres — durable store for agents, runs, and audit. - redis — pub/sub for the live event stream. the reference deployment is a docker-compose file plus a helm chart. both are kept in infra/. the minimal footprint is: - 1× api container (single replica is fine for hundreds of agents). - 1× web container. - 1× engine worker (scale horizontally per concurrent run). - postgres + redis (your choice — managed or in-cluster). run the model backend of your choice on the same network as the gateway. the catalog file (gateway.models.yaml) tells the gateway where to find each backend. for apple silicon, see mlx (apple silicon). for linux, the openai-compat adapter against ollama, vllm, llama.cpp, or lm studio is the path most operators take. the control plane needs a master "
  },
  {
    "path": "/docs/guides/local-models-mlx",
    "title": "Local models — MLX (Apple Silicon)",
    "summary": "Run frontier-quality reasoning on your laptop without leaving the box.",
    "headings": [
      "install + spin up the server",
      "register the model with the gateway",
      "eval-harness comparison",
      "constraints"
    ],
    "body": "native mlx_lm.server adapter for aldo ai. macos-on-apple-silicon's fastest local-llm path; linux and other platforms continue to use the openai-compat adapter against ollama, vllm, llama.cpp, or lm studio. > ollama, vllm, llama.cpp, and mlx are runtime backends — they're > the host process the gateway talks to. they are not llm providers > in the llm-agnostic sense; the routing layer above them still > resolves models by capability class. ```bash pip install mlx-lm # 0.21+ for tool-calling support python -m mlx_lm.server \\ --model mlx-community/qwen2.5-7b-instruct-4bit \\ --port 8081 ``` the server exposes /v1/chat/completions, /v1/embeddings, and a plain /health probe. the seed catalog (platform/gateway/fixtures/models.yaml) already contains illustrative mlx entries — operators override these with their own catalog. a minimal entry looks like: ```yaml - id: mlx-qwen2.5-7b-instruct-4bit provider: mlx providerkind: mlx locality: local capabilityclass: local-reasoning provides: [tool-use, function-calling, streaming, structured-output] privacyallowed: [public, internal, sensitive] effectivecontexttokens: 32768 cost: { usdpermtokin: 0, usdpermtokout: 0 } providerconfig: baseurl: http:/"
  },
  {
    "path": "/docs/sdks/python",
    "title": "Python SDK",
    "summary": "The official Python client for the ALDO AI control plane.",
    "headings": ["install", "quickstart", "authentication", "errors", "streaming", "coming soon"],
    "body": "> the python sdk is early. the core api surface is stable and > mirrors the zod contract package; advanced features (streaming > events, breakpoints) ship in a follow-up. ```bash pip install aldo-ai ``` ```python from aldo_ai import aldoclient client = aldoclient( base_url=\"https://app.aldo-ai.dev\", api_key=\"sk-aldo-...\", # or set aldo_api_key in env ) list agents agents = client.agents.list() for a in agents.agents: print(a.name, a.latest_version) run an agent in the playground result = client.playground.run( agent_name=\"changelog-writer\", input={\"prompt\": \"generate a changelog for v0.4.2\"}, ) print(result.run_id, result.status) ``` pass api_key= to the constructor or set aldo_api_key in the environment. keys are minted under settings → api keys; pick the narrowest scope your job needs. the client raises typed exceptions: - aldoautherror — 401 / 403. - aldovalidationerror — 400 / 422 with the field-level issues. - aldoratelimiterror — 429 with retry_after. - aldonotfounderror — 404. - aldoservererror — 5xx. all inherit from aldoerror, which carries the request id so you can grep the api logs for the failed call. run events stream over sse: ```python for event in client.runs.events"
  },
  {
    "path": "/docs/sdks/typescript",
    "title": "TypeScript SDK",
    "summary": "@aldo-ai/api-contract is the TypeScript SDK — Zod schemas + a typed fetch surface.",
    "headings": [
      "install",
      "authenticate",
      "list agents",
      "run in the playground",
      "stream events",
      "error shape",
      "schemas as the contract"
    ],
    "body": "the typescript sdk is the same @aldo-ai/api-contract package the control-plane web app uses. every endpoint has zod schemas for the request and response, and the package re-exports a typed fetch helper for client code. ```bash pnpm add @aldo-ai/api-contract ``` mint a key under settings → api keys, then: ```ts import { createclient } from '@aldo-ai/api-contract/client'; const client = createclient({ baseurl: 'https://app.aldo-ai.dev', apikey: process.env.aldo_api_key, }); ``` ```ts const { agents } = await client.agents.list(); for (const a of agents) { console.log(a.name, a.latestversion); } ``` ```ts const result = await client.playground.run({ agentname: 'changelog-writer', input: { prompt: 'generate a changelog for v0.4.2' }, }); console.log(result.runid, result.status); ``` ```ts const events = client.runs.events('run_abc123'); for await (const ev of events) { console.log(ev.type, ev.payload); } ``` every failure throws an apiclienterror carrying the http status, the structured error body, and the request id: ```ts import { apiclienterror } from '@aldo-ai/api-contract/client'; try { await client.agents.get('does-not-exist'); } catch (err) { if (err instanceof apiclienterror &&"
  },
  {
    "path": "/docs/changelog",
    "title": "Changelog",
    "summary": "Release history — newest first.",
    "headings": [
      "0.4.0 — 2026-04-26 (wave 15)",
      "0.3.0 — 2026-04 (wave 14)",
      "0.2.0 — 2026-03 (wave 13)",
      "0.1.0 — 2026-02 (wave 12)",
      "earlier"
    ],
    "body": "this page is the user-facing release history. internal milestones live in development_log.txt in the repository root. - documentation site. real, indexed docs at /docs. quickstart, concepts, guides, api reference, sdks, changelog, license. - cmd-k docs search. the command palette now searches doc pages alongside agents, runs, and models. - auto-generated api reference. every zod-schema'd endpoint in @aldo-ai/api-contract gets a generated reference page with curl, python, and typescript examples. - integrations. slack, discord, github, generic webhooks. rbac + secrets-encrypted at rest. - public share viewer. read-only run pages share-able with a signed url, with watermark and a cta bar. - empty-state illustrations for runs, agents, datasets, dashboards, alerts, sweeps, integrations, notifications. - api keys + rbac. per-key scopes, audit, expiry. - dashboards + alerts. first-class layouts, alert rules, silencing. - public marketing surface. /, /pricing, /about, /security, /design-partner. - command palette. cmd-k for navigation across agents, runs, and models. - theme tokens. light + dark mode driven by css custom properties. see development_log.txt in the repository for the full h"
  },
  {
    "path": "/docs/license",
    "title": "License",
    "summary": "Functional Source License 1.1 with Apache 2.0 future license.",
    "headings": [
      "abbreviation",
      "notice",
      "terms and conditions",
      "licensor (\"we\")",
      "the software",
      "license grant",
      "permitted purpose",
      "patents",
      "redistribution",
      "disclaimer",
      "trademarks",
      "grant of future license"
    ],
    "body": "functional source license, version 1.1, alv2 future license fsl-1.1-alv2 copyright 2026 aldo tech labs the party offering the software under these terms and conditions. the \"software\" is each version of the software that we make available under these terms and conditions, as indicated by our inclusion of these terms and conditions with the software. subject to your compliance with this license grant and the patents, redistribution and trademark clauses below, we hereby grant you the right to use, copy, modify, create derivative works, publicly perform, publicly display and redistribute the software for any permitted purpose identified below. a permitted purpose is any purpose other than a competing use. a competing use means making the software available to others in a commercial product or service that: 1. substitutes for the software; 2. substitutes for any other product or service we offer using the software that exists as of the date we make the software available; or 3. offers the same or substantially similar functionality as the software. permitted purposes specifically include using the software: 1. for your internal use and access; 2. for non-commercial education; 3. for n"
  },
  {
    "path": "/docs/api",
    "title": "API reference",
    "summary": "Auto-generated reference for every endpoint that has a Zod schema in @aldo-ai/api-contract.",
    "headings": ["base url", "authentication", "versioning", "error shape", "endpoints"],
    "body": "the control-plane http api is documented from a single source of truth: the zod schemas in @aldo-ai/api-contract. the build-time generator (apps/web/scripts/generate-api-docs.ts) reads every schema, emits one json spec per endpoint, and the docs surface renders that spec as a typed reference page with curl, python, and typescript examples. ``` https://app.aldo-ai.dev ``` for self-hosted deployments, replace this with your operator's base url. every endpoint (except /health and the public share viewer) requires a bearer token. mint a key under settings → api keys and pass it in the authorization header: ```bash curl https://app.aldo-ai.dev/api/auth-proxy/v1/agents \\ -h \"authorization: bearer $aldo_api_key\" ``` the current api is /v1/. breaking changes require a /v2/ sibling and a deprecation timeline announced in the changelog. additive changes (new optional fields, new endpoints) are not breaking. every failure responds with: ```json { \"error\": { \"code\": \"string\", \"message\": \"string\", \"issues\": [], \"request_id\": \"string\" } } ``` the code is stable; the message is human-friendly. issues carries zod field-level errors when applicable. request_id is the audit log identifier — use it w"
  },
  {
    "path": "/docs/api/delete-agents__name",
    "title": "DELETE /v1/agents/:name",
    "summary": "Delete resources under `agents`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "delete /v1/agents/:name delete resources under `agents`."
  },
  {
    "path": "/docs/api/delete-alerts__id",
    "title": "DELETE /v1/alerts/:id",
    "summary": "Delete resources under `alerts`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "delete /v1/alerts/:id delete resources under `alerts`."
  },
  {
    "path": "/docs/api/delete-annotations__id",
    "title": "DELETE /v1/annotations/:id",
    "summary": "Delete resources under `annotations`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "delete /v1/annotations/:id delete resources under `annotations`."
  },
  {
    "path": "/docs/api/delete-api-keys__id",
    "title": "DELETE /v1/api-keys/:id",
    "summary": "Delete resources under `api-keys`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "delete /v1/api-keys/:id delete resources under `api-keys`."
  },
  {
    "path": "/docs/api/delete-dashboards__id",
    "title": "DELETE /v1/dashboards/:id",
    "summary": "Delete resources under `dashboards`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "delete /v1/dashboards/:id delete resources under `dashboards`."
  },
  {
    "path": "/docs/api/delete-integrations__id",
    "title": "DELETE /v1/integrations/:id",
    "summary": "Delete resources under `integrations`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "delete /v1/integrations/:id delete resources under `integrations`."
  },
  {
    "path": "/docs/api/delete-invitations__id",
    "title": "DELETE /v1/invitations/:id",
    "summary": "Delete resources under `invitations`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "delete /v1/invitations/:id delete resources under `invitations`."
  },
  {
    "path": "/docs/api/delete-members__userId",
    "title": "DELETE /v1/members/:userId",
    "summary": "Delete resources under `members`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "delete /v1/members/:userid delete resources under `members`."
  },
  {
    "path": "/docs/api/delete-runs__id_breakpoints__bp",
    "title": "DELETE /v1/runs/:id/breakpoints/:bp",
    "summary": "Delete resources under `runs`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "delete /v1/runs/:id/breakpoints/:bp delete resources under `runs`."
  },
  {
    "path": "/docs/api/delete-secrets__name",
    "title": "DELETE /v1/secrets/:name",
    "summary": "Delete resources under `secrets`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "delete /v1/secrets/:name delete resources under `secrets`."
  },
  {
    "path": "/docs/api/delete-shares__id",
    "title": "DELETE /v1/shares/:id",
    "summary": "Delete resources under `shares`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "delete /v1/shares/:id delete resources under `shares`."
  },
  {
    "path": "/docs/api/delete-views__id",
    "title": "DELETE /v1/views/:id",
    "summary": "Delete resources under `views`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "delete /v1/views/:id delete resources under `views`."
  },
  {
    "path": "/docs/api/get-activity",
    "title": "GET /v1/activity",
    "summary": "Read resources under `activity`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/activity read resources under `activity`."
  },
  {
    "path": "/docs/api/get-admin_design-partner-applications",
    "title": "GET /v1/admin/design-partner-applications",
    "summary": "Read resources under `admin`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/admin/design-partner-applications read resources under `admin`."
  },
  {
    "path": "/docs/api/get-agents",
    "title": "GET /v1/agents",
    "summary": "Read resources under `agents`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/agents read resources under `agents`."
  },
  {
    "path": "/docs/api/get-agents__name",
    "title": "GET /v1/agents/:name",
    "summary": "Read resources under `agents`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/agents/:name read resources under `agents`."
  },
  {
    "path": "/docs/api/get-agents__name_versions",
    "title": "GET /v1/agents/:name/versions",
    "summary": "Read resources under `agents`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/agents/:name/versions read resources under `agents`."
  },
  {
    "path": "/docs/api/get-agents__name_versions__version",
    "title": "GET /v1/agents/:name/versions/:version",
    "summary": "Read resources under `agents`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/agents/:name/versions/:version read resources under `agents`."
  },
  {
    "path": "/docs/api/get-alerts",
    "title": "GET /v1/alerts",
    "summary": "Read resources under `alerts`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/alerts read resources under `alerts`."
  },
  {
    "path": "/docs/api/get-alerts__id",
    "title": "GET /v1/alerts/:id",
    "summary": "Read resources under `alerts`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/alerts/:id read resources under `alerts`."
  },
  {
    "path": "/docs/api/get-alerts__id_events",
    "title": "GET /v1/alerts/:id/events",
    "summary": "Read resources under `alerts`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/alerts/:id/events read resources under `alerts`."
  },
  {
    "path": "/docs/api/get-annotations",
    "title": "GET /v1/annotations",
    "summary": "Read resources under `annotations`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/annotations read resources under `annotations`."
  },
  {
    "path": "/docs/api/get-annotations_feed",
    "title": "GET /v1/annotations/feed",
    "summary": "Read resources under `annotations`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/annotations/feed read resources under `annotations`."
  },
  {
    "path": "/docs/api/get-api-keys",
    "title": "GET /v1/api-keys",
    "summary": "Read resources under `api-keys`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/api-keys read resources under `api-keys`."
  },
  {
    "path": "/docs/api/get-audit",
    "title": "GET /v1/audit",
    "summary": "Read resources under `audit`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/audit read resources under `audit`."
  },
  {
    "path": "/docs/api/get-billing_subscription",
    "title": "GET /v1/billing/subscription",
    "summary": "Read resources under `billing`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/billing/subscription read resources under `billing`."
  },
  {
    "path": "/docs/api/get-billing_usage",
    "title": "GET /v1/billing/usage",
    "summary": "Read resources under `billing`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/billing/usage read resources under `billing`."
  },
  {
    "path": "/docs/api/get-dashboards",
    "title": "GET /v1/dashboards",
    "summary": "Read resources under `dashboards`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/dashboards read resources under `dashboards`."
  },
  {
    "path": "/docs/api/get-dashboards__id",
    "title": "GET /v1/dashboards/:id",
    "summary": "Read resources under `dashboards`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/dashboards/:id read resources under `dashboards`."
  },
  {
    "path": "/docs/api/get-eval_suites",
    "title": "GET /v1/eval/suites",
    "summary": "Read resources under `eval`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/eval/suites read resources under `eval`."
  },
  {
    "path": "/docs/api/get-eval_suites__name",
    "title": "GET /v1/eval/suites/:name",
    "summary": "Read resources under `eval`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/eval/suites/:name read resources under `eval`."
  },
  {
    "path": "/docs/api/get-eval_sweeps",
    "title": "GET /v1/eval/sweeps",
    "summary": "Read resources under `eval`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/eval/sweeps read resources under `eval`."
  },
  {
    "path": "/docs/api/get-eval_sweeps__id",
    "title": "GET /v1/eval/sweeps/:id",
    "summary": "Read resources under `eval`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/eval/sweeps/:id read resources under `eval`."
  },
  {
    "path": "/docs/api/get-health",
    "title": "GET /health",
    "summary": "Read resources under ``.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /health read resources under ``."
  },
  {
    "path": "/docs/api/get-integrations",
    "title": "GET /v1/integrations",
    "summary": "Read resources under `integrations`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/integrations read resources under `integrations`."
  },
  {
    "path": "/docs/api/get-integrations__id",
    "title": "GET /v1/integrations/:id",
    "summary": "Read resources under `integrations`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/integrations/:id read resources under `integrations`."
  },
  {
    "path": "/docs/api/get-invitations",
    "title": "GET /v1/invitations",
    "summary": "Read resources under `invitations`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/invitations read resources under `invitations`."
  },
  {
    "path": "/docs/api/get-members",
    "title": "GET /v1/members",
    "summary": "Read resources under `members`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/members read resources under `members`."
  },
  {
    "path": "/docs/api/get-models",
    "title": "GET /v1/models",
    "summary": "Read resources under `models`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/models read resources under `models`."
  },
  {
    "path": "/docs/api/get-models_savings",
    "title": "GET /v1/models/savings",
    "summary": "Read resources under `models`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/models/savings read resources under `models`."
  },
  {
    "path": "/docs/api/get-notifications",
    "title": "GET /v1/notifications",
    "summary": "Read resources under `notifications`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/notifications read resources under `notifications`."
  },
  {
    "path": "/docs/api/get-observability_summary",
    "title": "GET /v1/observability/summary",
    "summary": "Read resources under `observability`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/observability/summary read resources under `observability`."
  },
  {
    "path": "/docs/api/get-public_share__slug",
    "title": "GET /v1/public/share/:slug",
    "summary": "Read resources under `public`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/public/share/:slug read resources under `public`."
  },
  {
    "path": "/docs/api/get-runs",
    "title": "GET /v1/runs",
    "summary": "Read resources under `runs`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/runs read resources under `runs`."
  },
  {
    "path": "/docs/api/get-runs__id",
    "title": "GET /v1/runs/:id",
    "summary": "Read resources under `runs`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/runs/:id read resources under `runs`."
  },
  {
    "path": "/docs/api/get-runs__id_breakpoints",
    "title": "GET /v1/runs/:id/breakpoints",
    "summary": "Read resources under `runs`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/runs/:id/breakpoints read resources under `runs`."
  },
  {
    "path": "/docs/api/get-runs__id_events",
    "title": "GET /v1/runs/:id/events",
    "summary": "Read resources under `runs`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/runs/:id/events read resources under `runs`."
  },
  {
    "path": "/docs/api/get-runs__id_tree",
    "title": "GET /v1/runs/:id/tree",
    "summary": "Read resources under `runs`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/runs/:id/tree read resources under `runs`."
  },
  {
    "path": "/docs/api/get-runs_compare",
    "title": "GET /v1/runs/compare",
    "summary": "Read resources under `runs`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/runs/compare read resources under `runs`."
  },
  {
    "path": "/docs/api/get-runs_search",
    "title": "GET /v1/runs/search",
    "summary": "Read resources under `runs`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/runs/search read resources under `runs`."
  },
  {
    "path": "/docs/api/get-secrets",
    "title": "GET /v1/secrets",
    "summary": "Read resources under `secrets`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/secrets read resources under `secrets`."
  },
  {
    "path": "/docs/api/get-shares",
    "title": "GET /v1/shares",
    "summary": "Read resources under `shares`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/shares read resources under `shares`."
  },
  {
    "path": "/docs/api/get-sse_events",
    "title": "GET /v1/sse/events",
    "summary": "Read resources under `sse`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/sse/events read resources under `sse`."
  },
  {
    "path": "/docs/api/get-views",
    "title": "GET /v1/views",
    "summary": "Read resources under `views`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "get /v1/views read resources under `views`."
  },
  {
    "path": "/docs/api/patch-admin_design-partner-applications__id",
    "title": "PATCH /v1/admin/design-partner-applications/:id",
    "summary": "Update resources under `admin`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "patch /v1/admin/design-partner-applications/:id update resources under `admin`."
  },
  {
    "path": "/docs/api/patch-alerts__id",
    "title": "PATCH /v1/alerts/:id",
    "summary": "Update resources under `alerts`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "patch /v1/alerts/:id update resources under `alerts`."
  },
  {
    "path": "/docs/api/patch-annotations__id",
    "title": "PATCH /v1/annotations/:id",
    "summary": "Update resources under `annotations`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "patch /v1/annotations/:id update resources under `annotations`."
  },
  {
    "path": "/docs/api/patch-dashboards__id",
    "title": "PATCH /v1/dashboards/:id",
    "summary": "Update resources under `dashboards`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "patch /v1/dashboards/:id update resources under `dashboards`."
  },
  {
    "path": "/docs/api/patch-integrations__id",
    "title": "PATCH /v1/integrations/:id",
    "summary": "Update resources under `integrations`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "patch /v1/integrations/:id update resources under `integrations`."
  },
  {
    "path": "/docs/api/patch-members__userId",
    "title": "PATCH /v1/members/:userId",
    "summary": "Update resources under `members`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "patch /v1/members/:userid update resources under `members`."
  },
  {
    "path": "/docs/api/patch-views__id",
    "title": "PATCH /v1/views/:id",
    "summary": "Update resources under `views`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "patch /v1/views/:id update resources under `views`."
  },
  {
    "path": "/docs/api/post-agents",
    "title": "POST /v1/agents",
    "summary": "Create or invoke resources under `agents`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/agents create or invoke resources under `agents`."
  },
  {
    "path": "/docs/api/post-agents__name_check",
    "title": "POST /v1/agents/:name/check",
    "summary": "Create or invoke resources under `agents`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/agents/:name/check create or invoke resources under `agents`."
  },
  {
    "path": "/docs/api/post-agents__name_promote",
    "title": "POST /v1/agents/:name/promote",
    "summary": "Create or invoke resources under `agents`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/agents/:name/promote create or invoke resources under `agents`."
  },
  {
    "path": "/docs/api/post-agents__name_set-current",
    "title": "POST /v1/agents/:name/set-current",
    "summary": "Create or invoke resources under `agents`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/agents/:name/set-current create or invoke resources under `agents`."
  },
  {
    "path": "/docs/api/post-alerts",
    "title": "POST /v1/alerts",
    "summary": "Create or invoke resources under `alerts`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/alerts create or invoke resources under `alerts`."
  },
  {
    "path": "/docs/api/post-alerts__id_silence",
    "title": "POST /v1/alerts/:id/silence",
    "summary": "Create or invoke resources under `alerts`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/alerts/:id/silence create or invoke resources under `alerts`."
  },
  {
    "path": "/docs/api/post-alerts__id_test",
    "title": "POST /v1/alerts/:id/test",
    "summary": "Create or invoke resources under `alerts`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/alerts/:id/test create or invoke resources under `alerts`."
  },
  {
    "path": "/docs/api/post-annotations",
    "title": "POST /v1/annotations",
    "summary": "Create or invoke resources under `annotations`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/annotations create or invoke resources under `annotations`."
  },
  {
    "path": "/docs/api/post-annotations__id_reactions",
    "title": "POST /v1/annotations/:id/reactions",
    "summary": "Create or invoke resources under `annotations`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/annotations/:id/reactions create or invoke resources under `annotations`."
  },
  {
    "path": "/docs/api/post-api-keys",
    "title": "POST /v1/api-keys",
    "summary": "Create or invoke resources under `api-keys`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/api-keys create or invoke resources under `api-keys`."
  },
  {
    "path": "/docs/api/post-api-keys__id_revoke",
    "title": "POST /v1/api-keys/:id/revoke",
    "summary": "Create or invoke resources under `api-keys`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/api-keys/:id/revoke create or invoke resources under `api-keys`."
  },
  {
    "path": "/docs/api/post-billing_checkout",
    "title": "POST /v1/billing/checkout",
    "summary": "Create or invoke resources under `billing`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/billing/checkout create or invoke resources under `billing`."
  },
  {
    "path": "/docs/api/post-billing_portal",
    "title": "POST /v1/billing/portal",
    "summary": "Create or invoke resources under `billing`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/billing/portal create or invoke resources under `billing`."
  },
  {
    "path": "/docs/api/post-billing_webhook",
    "title": "POST /v1/billing/webhook",
    "summary": "Create or invoke resources under `billing`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/billing/webhook create or invoke resources under `billing`."
  },
  {
    "path": "/docs/api/post-dashboards",
    "title": "POST /v1/dashboards",
    "summary": "Create or invoke resources under `dashboards`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/dashboards create or invoke resources under `dashboards`."
  },
  {
    "path": "/docs/api/post-dashboards__id_data",
    "title": "POST /v1/dashboards/:id/data",
    "summary": "Create or invoke resources under `dashboards`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/dashboards/:id/data create or invoke resources under `dashboards`."
  },
  {
    "path": "/docs/api/post-design-partners_apply",
    "title": "POST /v1/design-partners/apply",
    "summary": "Create or invoke resources under `design-partners`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/design-partners/apply create or invoke resources under `design-partners`."
  },
  {
    "path": "/docs/api/post-eval_suites",
    "title": "POST /v1/eval/suites",
    "summary": "Create or invoke resources under `eval`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/eval/suites create or invoke resources under `eval`."
  },
  {
    "path": "/docs/api/post-eval_sweeps",
    "title": "POST /v1/eval/sweeps",
    "summary": "Create or invoke resources under `eval`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/eval/sweeps create or invoke resources under `eval`."
  },
  {
    "path": "/docs/api/post-integrations",
    "title": "POST /v1/integrations",
    "summary": "Create or invoke resources under `integrations`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/integrations create or invoke resources under `integrations`."
  },
  {
    "path": "/docs/api/post-integrations__id_test",
    "title": "POST /v1/integrations/:id/test",
    "summary": "Create or invoke resources under `integrations`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/integrations/:id/test create or invoke resources under `integrations`."
  },
  {
    "path": "/docs/api/post-invitations",
    "title": "POST /v1/invitations",
    "summary": "Create or invoke resources under `invitations`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/invitations create or invoke resources under `invitations`."
  },
  {
    "path": "/docs/api/post-invitations__id_revoke",
    "title": "POST /v1/invitations/:id/revoke",
    "summary": "Create or invoke resources under `invitations`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/invitations/:id/revoke create or invoke resources under `invitations`."
  },
  {
    "path": "/docs/api/post-invitations_accept",
    "title": "POST /v1/invitations/accept",
    "summary": "Create or invoke resources under `invitations`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/invitations/accept create or invoke resources under `invitations`."
  },
  {
    "path": "/docs/api/post-notifications__id_mark-read",
    "title": "POST /v1/notifications/:id/mark-read",
    "summary": "Create or invoke resources under `notifications`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/notifications/:id/mark-read create or invoke resources under `notifications`."
  },
  {
    "path": "/docs/api/post-notifications_mark-all-read",
    "title": "POST /v1/notifications/mark-all-read",
    "summary": "Create or invoke resources under `notifications`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/notifications/mark-all-read create or invoke resources under `notifications`."
  },
  {
    "path": "/docs/api/post-playground_run",
    "title": "POST /v1/playground/run",
    "summary": "Create or invoke resources under `playground`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/playground/run create or invoke resources under `playground`."
  },
  {
    "path": "/docs/api/post-runs",
    "title": "POST /v1/runs",
    "summary": "Create or invoke resources under `runs`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/runs create or invoke resources under `runs`."
  },
  {
    "path": "/docs/api/post-runs__id_breakpoints",
    "title": "POST /v1/runs/:id/breakpoints",
    "summary": "Create or invoke resources under `runs`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/runs/:id/breakpoints create or invoke resources under `runs`."
  },
  {
    "path": "/docs/api/post-runs__id_continue",
    "title": "POST /v1/runs/:id/continue",
    "summary": "Create or invoke resources under `runs`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/runs/:id/continue create or invoke resources under `runs`."
  },
  {
    "path": "/docs/api/post-runs__id_edit-and-resume",
    "title": "POST /v1/runs/:id/edit-and-resume",
    "summary": "Create or invoke resources under `runs`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/runs/:id/edit-and-resume create or invoke resources under `runs`."
  },
  {
    "path": "/docs/api/post-runs__id_swap-model",
    "title": "POST /v1/runs/:id/swap-model",
    "summary": "Create or invoke resources under `runs`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/runs/:id/swap-model create or invoke resources under `runs`."
  },
  {
    "path": "/docs/api/post-runs_bulk",
    "title": "POST /v1/runs/bulk",
    "summary": "Create or invoke resources under `runs`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/runs/bulk create or invoke resources under `runs`."
  },
  {
    "path": "/docs/api/post-secrets",
    "title": "POST /v1/secrets",
    "summary": "Create or invoke resources under `secrets`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/secrets create or invoke resources under `secrets`."
  },
  {
    "path": "/docs/api/post-shares",
    "title": "POST /v1/shares",
    "summary": "Create or invoke resources under `shares`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/shares create or invoke resources under `shares`."
  },
  {
    "path": "/docs/api/post-shares__id_revoke",
    "title": "POST /v1/shares/:id/revoke",
    "summary": "Create or invoke resources under `shares`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/shares/:id/revoke create or invoke resources under `shares`."
  },
  {
    "path": "/docs/api/post-tenants_me_seed-default",
    "title": "POST /v1/tenants/me/seed-default",
    "summary": "Create or invoke resources under `tenants`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/tenants/me/seed-default create or invoke resources under `tenants`."
  },
  {
    "path": "/docs/api/post-views",
    "title": "POST /v1/views",
    "summary": "Create or invoke resources under `views`.",
    "headings": ["request", "response", "example", "errors"],
    "body": "post /v1/views create or invoke resources under `views`."
  }
]
