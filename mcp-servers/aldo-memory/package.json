{
  "name": "@aldo-ai/mcp-memory",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ALDO AI first-party MCP server: scope-aware memory store (private/project/org/session) with filesystem persistence.",
  "license": "FSL-1.1-ALv2",
  "main": "./src/index.ts",
  "bin": {
    "aldo-mcp-memory": "./dist/index.js"
  },
  "exports": {
    ".": "./src/index.ts",
    "./server": "./src/server.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "start": "node --import=tsx/esm src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.23.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3",
    "vitest": "^4.1.5"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
